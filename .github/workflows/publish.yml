name: Publish and commit bottles

on:
  pull_request_target:
    types:
      - labeled

env:
  HOMEBREW_DEVELOPER: 1
  HOMEBREW_NO_ANALYTICS: 1
  BOTTLE_BRANCH: ${{ github.event.pull_request.head.ref }}
  BOTTLES_DIR: ${{ github.workspace }}/bottles

defaults:
  run:
    shell: bash -xeuo pipefail {0}

jobs:
  publish:
    if: contains(github.event.pull_request.labels.*.name, 'pr-pull')
    runs-on: macos-26
    permissions:
      actions: read
      checks: read
      contents: write
      issues: read
      packages: write
      pull-requests: write
      attestations: write
      id-token: write
      artifact-metadata: write
    steps:
      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@main
        with:
          core: false
          cask: false
          token: ${{ secrets.GITHUB_TOKEN }}

      - id: get-run-id
        env:
          GH_TOKEN: ${{ secrets.AUTOBUMP_TOKEN }}
        run: |
          runid=$(gh run list --branch "$BOTTLE_BRANCH" --workflow tests.yml --limit 1 --json databaseId --jq '.[].databaseId')
          echo "run-id=${runid}" >> "${GITHUB_OUTPUT}"

      - name: Download bottles from GitHub Actions
        uses: actions/download-artifact@main
        with:
          pattern: bottles_*
          path: ${{ env.BOTTLES_DIR }}
          merge-multiple: true
          github-token: ${{ secrets.AUTOBUMP_TOKEN }}
          run-id: ${{ steps.get-run-id.outputs.run-id }}

      - name: Configure Git user
        id: git-user-config
        uses: Homebrew/actions/git-user-config@main
        with:
          username: ${{ github.repository_owner }}

      - name: Generate build provenance
        uses: actions/attest@main
        with:
          subject-path: ${{ env.BOTTLES_DIR }}/*.tar.gz

      - name: Pull bottles
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_GITHUB_PACKAGES_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_GITHUB_PACKAGES_USER: ${{ github.repository_owner }}
          PULL_REQUEST: ${{ github.event.pull_request.number }}
        run: |
          if [ ! -e "/etc/containers" ]
          then
            sudo mkdir -p /etc/containers
            sudo curl -sSL -o /etc/containers/policy.json https://raw.githubusercontent.com/podman-container-tools/skopeo/refs/heads/main/default-policy.json
          fi
          brew tap otsge/draft
          brew trust otsge/draft
          brew pr-pull --debug --verbose --tap="$GITHUB_REPOSITORY" "$PULL_REQUEST"

      - name: Push commits
        uses: Homebrew/actions/git-try-push@main
        with:
          branch: main
        env:
          GIT_COMMITTER_NAME: BrewTestBot
          GIT_COMMITTER_EMAIL: 1589480+BrewTestBot@users.noreply.github.com

      - name: Delete branch
        if: github.event.pull_request.head.repo.fork == false
        run: git push --delete origin "${BOTTLE_BRANCH}"
